name: Compile / AVR

on:
  pull_request:
    paths:
      - 'src'
      - '.github/workflows/compile_avr.yml'

  push:
    paths:
      - 'src'
      - '.github/workflows/compile_avr.yml'

jobs:
  avr-uno:
    runs-on: ubuntu-latest
    
    strategy:
      matrix:
        board:
          - fqbn: arduino:avr:uno
            platforms: |
              - name: arduino:avr
                version: 1.8.6
            libraries: |
              - name: Servo
                version: 1.1.8
              - name: LiquidCrystal
                version: 1.0.7
              - name: IRremote
                version: 4.0.0
              - name: Ethernet
                version: 2.0.1
              - name: WiFi
                version: 1.2.7
        sketches:
          - name: serial
            path: dino_serial_0.12.0
          - name: ethernet
            path: dino_ethernet_0.12.0
          - name: wifi
            path: dino_wifi_0.12.0

    steps:
      - name: Checkout
        uses: actions/checkout@v3
      
      - name: Generate
        run: ./bin/dino sketch ${{ matrix.sketches.name }} --target mega
        shell: bash
        
      - name: Compile Arduino Sketches
        uses: arduino/compile-sketches@v1.0.1
        with:
          platforms: ${{ matrix.board.platforms }}
          fqbn: ${{ matrix.board.fqbn }}
          libraries: ${{ matrix.board.libraries }}
          sketch-paths: |
            - ${{ matrix.sketches.path }}
            